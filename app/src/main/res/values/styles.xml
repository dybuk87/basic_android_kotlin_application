<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="health_toolbar">
        <item name="android:background">#cccccc</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:theme">@style/ThemeOverlay.AppCompat.ActionBar</item>
        <item name="contentInsetStartWithNavigation">0dp</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
    </style>


    <style name="large_title">
        <item name="android:textSize">20sp</item>
    </style>

    <style name="large_title_black" parent="large_title">
        <item name="android:textColor">#ff000000</item>
    </style>

    <style name="large_subtitle_gray" parent="large_title">
        <item name="android:textColor">#ffaaaaaa</item>
    </style>

    <declare-styleable name="circle_view">
        <attr name="circleColor" format="color|reference" />
    </declare-styleable>

    <declare-styleable name="circle_initial_view">
        <attr name="android:textAppearance" />
    </declare-styleable>

</resources>
